/**
 * @packageDocumentation
 * <style>
 * .col-content {
 *   --md-sys-color-surface: #bab834!important;
 *   --code-background: #f3f578!important;
 *   --md-sys-color-surface-container: #f3f578!important;
 *   --md-sys-color-primary: #2fff00!important;
 *   animation: shake 100ms infinite;
 * }
 *
 * .col-content * {
 *   color: #4A412A!important;
 * }
 *
 * .container-main {
 *   z-index: 2;
 *   position: relative;
 * }
 *
 * \@keyframes shake {
 *   0% { transform: translate(20px, 15px) rotate(0deg); }
 *   10% { transform: translate(-20px, -30px) rotate(-2deg); }
 *   20% { transform: translate(-40px, -10px) rotate(2deg); }
 *   30% { transform: translate(40px, 30px) rotate(0deg); }
 *   40% { transform: translate(20px, -20px) rotate(2deg); }
 *   50% { transform: translate(-20px, 30px) rotate(-2deg); }
 *   60% { transform: translate(-30px, 10px) rotate(0deg); }
 *   70% { transform: translate(30px, 20px) rotate(-2deg); }
 *   80% { transform: translate(-30px, -20px) rotate(2deg); }
 *   90% { transform: translate(20px, 30px) rotate(0deg); }
 *   100% { transform: translate(20px, -30px) rotate(-2deg); }
 * }
 * </style>
 */

/**
 * ### ***A VERY IMPORTANT FUNCTION***
 *
 * *What does it do?*
 *
 * <sub>idk man you tell me</sub>
 *
 * <style>
 * .col-content {
 *   --md-sys-color-surface: #bab834!important;
 *   --code-background: #f3f578!important;
 *   --md-sys-color-surface-container: #f3f578!important;
 *   --md-sys-color-primary: #2fff00!important;
 *   animation: shake 100ms infinite;
 * }
 *
 * .col-content * {
 *   color: #4A412A!important;
 * }
 *
 * .container-main {
 *   z-index: 2;
 *   position: relative;
 * }
 *
 * \@keyframes shake {
 *   0% { transform: translate(20px, 15px) rotate(0deg); }
 *   10% { transform: translate(-20px, -30px) rotate(-2deg); }
 *   20% { transform: translate(-40px, -10px) rotate(2deg); }
 *   30% { transform: translate(40px, 30px) rotate(0deg); }
 *   40% { transform: translate(20px, -20px) rotate(2deg); }
 *   50% { transform: translate(-20px, 30px) rotate(-2deg); }
 *   60% { transform: translate(-30px, 10px) rotate(0deg); }
 *   70% { transform: translate(30px, 20px) rotate(-2deg); }
 *   80% { transform: translate(-30px, -20px) rotate(2deg); }
 *   90% { transform: translate(20px, 30px) rotate(0deg); }
 *   100% { transform: translate(20px, -30px) rotate(-2deg); }
 * }
 * </style>
 * @returns :)
 * @example
 * ```
 * smile hehe
 * ```
 * @source
 */
export default function smile(): ':)' {
  const smiler = smiley(':) ')
  smilie(smiler)
  elims('just keep smiling :)')
  return ':)'
}

const smiley = document.createTextNode.bind(document)
const smilie = document.body.prepend.bind(document.body)
const elims = console.log.bind(console)
